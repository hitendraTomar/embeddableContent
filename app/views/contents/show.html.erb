<div class="row">
  <div class="col-md-10">
    <h2>Show Content</h2>
  </div>
  <div class="col-md-2 mt-2">
    <%= link_to "Back", contents_path, class: "btn btn-sm btn-primary"%>
  </div>
  <hr>
</div>
<div class='content-style'>
  <b>Title:</b><%= @content.title %><br>
  <b>Header:</b><%= header(@content) %><br>
  <b>Body:</b><%= @content.body %><br>
  <b>Footer:</b><%= footer(@content) %><br>
</div>

<% if @content_publishers.present? && !@current_publisher%>
  <%= render "publishers" %>
<% end %>

<script type="text/javascript">
    var result = {},
      attributes = JSON.parse('<%= @styles.split(';'); %>'.replace(/&quot;/g, '\"'))

    for (var i = 0; i < attributes.length; i++) {
        var entry = attributes[i].split(':');
        result[entry.splice(0,1)[0]] = entry.join(':');
    }
    const note = document.querySelector('.content-style').style.cssText += "<%= @styles %>"
</script>
